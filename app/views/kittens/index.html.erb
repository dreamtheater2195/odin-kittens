<h1>List all the kittens</h1>
<ul>
  <% @kittens.each do |kitten| %>
    <li>
      <%= kitten.name %>
      <%= link_to "delete", kitten, method: :delete, data: {confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>
<%= link_to "Create new kitten",new_kitten_path %>
